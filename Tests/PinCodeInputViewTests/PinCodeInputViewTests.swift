import XCTest
@testable import PinCodeInputView

final class PinCodeInputViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PinCodeInputView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
